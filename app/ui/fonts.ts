import { Roboto, Rubik, Poppins } from 'next/font/google';

export const robotto = Roboto({
  weight: ['400'],
  subsets: ['latin'],
});
export const rubik = Rubik({ weight: ['400'], subsets: ['latin'] });

export const poppins = Poppins({ weight: ['400'], subsets: ['latin'] });
